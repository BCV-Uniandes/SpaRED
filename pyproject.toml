[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "SpaRED"
authors = [{name = "Placeholder", email = "Placeholder@Placeholder"}]
dynamic = ["version", "description"]
